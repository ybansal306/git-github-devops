  git remote add origin https://github.com/ybansal306/git-github-devops.git
    git remote -v
    git push origin master
    git push origin master
   git config --global user.name
   git config --global user.email
   git config --global user.name "Yash Bansal"
   git config --global user.email "ybansal306@gmail.com"
   git config --global user.name
   git config --global user.email
   echo "update" >> file.txt...
  git push --set-upstream origin master
  git push
